#ifndef PHYSICS_H
#define PHYSICS_H

namespace physics {
    enum class CollisionMode {
        Elastic,
        Inelastic,
        Collisionless
    };

    // double dt = 0.01;
    // double G = 100000.0;

    // void update();
}

#endif